"""
DiscoveryManager integration will be tested against a real SSH session.

This placeholder exists because the DiscoveryManager now depends on a
SessionManager rather than a standalone mock function.
"""

print("DiscoveryManager will be tested using a live SSH session.")