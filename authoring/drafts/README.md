# Draft schema entries

Output staging area for `tools/schema_drafter.py`. Files here are drafts only — never read by the app, never committed automatically. Review a draft, merge it into the matching file under `src/configbridge/schema/vendors/`, then `git add`/`git commit` it yourself. Once committed, it's an ordinary schema entry with no distinction from a hand-written one.
